[package]
name        = "um-vfs-abi"
version     = "0.1.0"
edition      = "2024"
rust-version = "1.92"
license     = "Apache-2.0"
description = "Crux OS VFS IPC protocol -- shared between vfs server and clients."

[lib]
name = "um_vfs_abi"
path = "lib.rs"

[dependencies]
zigbone-abi = { git = "https://github.com/crux-os/zigbone-abi.git" }
